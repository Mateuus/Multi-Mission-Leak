//Find your query... and add "voted" to what is queried. like

_query = switch(_side) do {
	case civilian: {format["SELECT name, /* blah blah blah*/ voted FROM players //etc. etc. etc.
};

//add it to every side, not just civilian